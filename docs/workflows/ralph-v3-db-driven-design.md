# Ralph design — superseded by v4

**This document is deprecated.** The source of truth is:

- **Agentic prompt (v4):** `.cursor/commands/agents/ralph.md`
- **Design doc:** `docs/workflows/ralph-design.md`

Ralph v4 uses a **single workflow (Cortex only)**; file-based mode and previous prompt versions (v2, v3) are no longer used. See `ralph-design.md` for the current flow, signals, and PRD attribute mapping.
