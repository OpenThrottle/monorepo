---
id: 11-ralph-one-task
title: Ralph — run a plan one task at a time
format: short
status: draft
release: 5
recording: replay
titleCard: ['Ralph runs a plan', 'one task at a time']
spokenWords: 67
blockedOn: []
tags:
  - openthrottle
  - ai agents
  - coding agents
  - automation
  - developer tools
---

**Replay.** The run's output stream is pre-baked by the demo seed; the flow drives
the UI that renders it. Recording a live model call here would produce a different
video every take and a failed take whenever the model wanders.

## Beats

| t    | on-screen action                                                 | narration                                                              |
| ---- | ---------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 0:00 | Plan detail, one task IN_PROGRESS, output streaming below it.    | One task in progress. Not four. That is the whole discipline.          |
| 0:09 | Highlight the task list — one in progress, the rest pending.     | Ralph takes the lowest task that is not done, and works only that one. |
| 0:18 | Output scrolls; a validation command runs and passes.            | It does the work, then it validates. Lint, types, tests.               |
| 0:28 | The task flips to COMPLETED; the next task flips to IN_PROGRESS. | Only when that passes does the task close and the next one open.       |
| 0:38 | Scroll to the commit for the finished task.                      | Each task gets its own commit.                                         |
| 0:45 | Hold on the plan; two tasks now complete.                        | So a failure costs you one task, not an afternoon of tangled changes.  |
| 0:53 | Outro card.                                                      |                                                                        |
