/**
 * Streaming OpenAI-compatible chat completions against a discovered local
 * endpoint. Pairs with `model-discovery`: discover an endpoint, then stream a
 * completion from one of its models.
 */
export * from './stream.js';
