import * as React from 'react';
import classnames from 'classnames';

export interface OpenThrottleServerHealthBannerProps {
  readonly className?: string;
}

export const OpenThrottleServerHealthBanner = (
  props: OpenThrottleServerHealthBannerProps,
) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="OpenThrottleServerHealthBanner"
    >
      <h2>OpenThrottleServerHealthBanner</h2>
    </div>
  );
};
