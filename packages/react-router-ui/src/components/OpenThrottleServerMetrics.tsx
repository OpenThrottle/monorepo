import * as React from 'react';
import classnames from 'classnames';

export interface OpenThrottleServerMetricsProps {
  readonly className?: string;
}

export const OpenThrottleServerMetrics = (
  props: OpenThrottleServerMetricsProps,
) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="OpenThrottleServerMetrics"
    >
      <h2>OpenThrottleServerMetrics</h2>
    </div>
  );
};
