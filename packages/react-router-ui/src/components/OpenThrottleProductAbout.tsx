import * as React from 'react';

export interface OpenThrottleProductAboutProps {
  // className?: string;
}

export const OpenThrottleProductAbout = (
  _props: OpenThrottleProductAboutProps,
): React.ReactElement => {
  // const {} = props;

  // Hooks
  const [_bool, _setBool] = React.useState(false);

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className="p-4" data-testid="OpenThrottleProductAbout">
      <h2>OpenThrottleProductAbout</h2>
    </div>
  );
};
