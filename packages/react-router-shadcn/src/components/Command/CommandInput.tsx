import * as React from 'react';
import classnames from 'classnames';

export interface CommandInputProps {
  readonly className?: string;
}

export const CommandInput = (props: CommandInputProps): React.ReactElement => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="CommandInput">
      <h2>CommandInput</h2>
    </div>
  );
};
