/**
 * @description Shared GraphQL Codegen types for OpenThrottle packages and apps.
 * Runtime plugins (`client` preset, `typescript-validation-schema`) are declared on
 * `@openthrottle/graphql-codegen` so the monorepo hoists a single dependency set.
 */
export type { CodegenConfig } from '@graphql-codegen/cli';
