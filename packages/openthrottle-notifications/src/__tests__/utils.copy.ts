import { describe, it, expect } from 'vitest';
// import { NOTIFICATION_EVENT_NAMES } from '../utils';

describe.skip('utils', () => {
  describe('currently skipped', () => {
    it('should xxxx', () => {
      expect(true).toBe(true);
    });
  });
});
