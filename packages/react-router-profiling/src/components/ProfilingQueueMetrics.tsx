import * as React from 'react';
// import classnames from 'classnames';

export interface ProfilingQueueMetricsProps {
  // className?: string;
}

export const ProfilingQueueMetrics = (
  _props: ProfilingQueueMetricsProps,
): React.ReactElement => {
  // const {  } = props;

  // Hooks
  const [_bool, _setBool] = React.useState(false);

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className="p-4" data-testid="ProfilingQueueMetrics">
      <h2>ProfilingQueueMetrics</h2>
    </div>
  );
};
