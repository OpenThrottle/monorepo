import * as React from 'react';
// import classnames from 'classnames';

export interface ProfilingServerMetricsProps {
  // className?: string;
}

export const ProfilingServerMetrics = (
  _props: ProfilingServerMetricsProps,
): React.ReactElement => {
  // const {  } = props;

  // Hooks
  const [_bool, _setBool] = React.useState(false);

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className="p-4" data-testid="ProfilingServerMetrics">
      <h2>ProfilingServerMetrics</h2>
    </div>
  );
};
