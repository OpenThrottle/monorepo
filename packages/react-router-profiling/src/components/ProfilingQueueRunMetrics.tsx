import * as React from 'react';
// import classnames from 'classnames';

export interface ProfilingQueueRunMetricsProps {
  // className?: string;
}

export const ProfilingQueueRunMetrics = (
  _props: ProfilingQueueRunMetricsProps,
): React.ReactElement => {
  // const {  } = props;

  // Hooks
  const [_bool, _setBool] = React.useState(false);

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className="p-4" data-testid="ProfilingQueueRunMetrics">
      <h2>ProfilingQueueRunMetrics</h2>
    </div>
  );
};
