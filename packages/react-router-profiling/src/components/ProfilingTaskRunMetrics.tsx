import * as React from 'react';
// import classnames from 'classnames';

export interface ProfilingTaskRunMetricsProps {
  // className?: string;
}

export const ProfilingTaskRunMetrics = (
  _props: ProfilingTaskRunMetricsProps,
): React.ReactElement => {
  // const {  } = props;

  // Hooks
  const [_bool, _setBool] = React.useState(false);

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className="p-4" data-testid="ProfilingTaskRunMetrics">
      <h2>ProfilingTaskRunMetrics</h2>
    </div>
  );
};
