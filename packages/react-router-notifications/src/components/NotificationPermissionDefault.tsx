import * as React from 'react';
import classnames from 'classnames';

export interface NotificationPermissionDefaultProps {
  readonly className?: string;
}

export const NotificationPermissionDefault = (
  props: NotificationPermissionDefaultProps,
) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="NotificationPermissionDefault"
    >
      <h2>NotificationPermissionDefault</h2>
    </div>
  );
};
