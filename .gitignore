#############################################################################
# Folders
#############################################################################
.cache
# Phase 1 skill-usage JSONL lives under .cache/skill-usage/ (covered by .cache)
.langgraph_api
.nx
.openthrottle
.pnpm-store
.vercel
build
coverage
design/*
dist
node_modules
repositories/*
services/*
storybook-static
tmp
videos

#############################################################################
# Files
#############################################################################
.cursor/rules/nx-rules.mdc
.DS_Store
.env*
.github/instructions/nx.instructions.md
.vscode/settings.json
*.cpuprofile
*.d.ts.map
*.tsbuildinfo
# Per-worktree generated artifacts (see scripts/worktree_ports.sh, setup_worktree.sh)
.worktree-ports
docker-compose.worktree.yml
**/__generated__/**/*
databases/backups/**/*.sql
databases/backups/**/*.zip
# Local Postgres seed dump — a full pg_dump of a dev's own database. Kept out of
# the repo so personal plans/tasks/notes and credential hashes never leak into a
# public checkout. The fresh/public DB is seeded by scripts instead: migrate +
# bootstrap-default-user + bootstrap-service-accounts (see databases/README.md).
# A locally-generated databases/seed.sql is still baked into your own postgres
# image (see .dockerignore) — it just never gets committed.
databases/seed.sql
# Durable local copy of the values the bootstrap scripts otherwise print only
# once (service-account tokens + default-user login). Holds credential material,
# local-only, never committed — same rationale as databases/seed.sql above. NOT
# covered by the .env* glob (it does not start with .env), so it is listed here
# explicitly. See scripts/local-secrets-file.ts and databases/README.md.
.bootstrap-secrets.local
vite.config.*.timestamp*
vite.config.ts.timestamp*
vitest.config.*.timestamp*
vitest.config.ts.timestamp*

#############################################################################
# Agent Specifics (Claude, Cursor, etc...)
#############################################################################
.claude.json
.claude/backups/
.claude/debug/
.claude/projects/
.claude/sessions/
.claude/settings.local.json
.claude/worktrees
CLAUDE.local.md

.cursor/cli-config.json
# Per-developer Cursor MCP config (may hold absolute paths / tokens). The
# committed SSOT is .cursor/mcp.json — see docs/openthrottle/mcp-registration.md.
# .cursor/mcp.json

#############################################################################
# Includes
#############################################################################
!.cursor/commands
!.cursor/rules/cursor-commands-github.mdc
!.env.default
!.gitkeep

# Tracking these in here as we flush the workflows out
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

# Start 🔄 Managed by OpenThrottle ot-skill-sync
.agents/skills/*
!.agents/skills/*/
.claude/skills/
.gemini/skills/
# End 🔄 Managed by OpenThrottle ot-skill-sync
