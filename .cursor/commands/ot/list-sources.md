# Instructions

Your job is to list what plans and sources are in the Cortex plans knowledge base using the ai-mcp MCP server.

## Rules

- **ALWAYS** follow the rules in `.cursor/rules/commands/cortex.mdc`
- **ALWAYS** use **`list_sources`** from the ai-mcp MCP server
- **ALWAYS** report the sources (plan, task) and the list of plan titles (and optionally ids) in a clear, readable format
