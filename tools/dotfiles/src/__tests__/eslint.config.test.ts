import { describe, expect, test } from 'vitest';

describe('eslint.config', () => {
  // let component: RenderResult;
  // let props: GlobalApplicationProps;

  // beforeEach(() => {
  //   component = render(<RoutesStub />);
  // });

  test('should xxxx', () => {
    expect(true).toBe(true);
  });
});
