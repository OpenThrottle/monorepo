export const PLAN_RUNS_STALE_SWEEP_QUEUE_NAME = 'Plan Runs Stale Sweep';

/** Max stale plan_runs rows settled per sweep (indexed cutoff scan, batch-capped). */
export const PLAN_RUNS_STALE_SWEEP_BATCH_SIZE = 200;
