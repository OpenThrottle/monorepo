/** Checkout route/config constants. Extend as needed. */
export const CHECKOUT_ROUTE_PREFIX = '/checkout';

/** Search param for plan id when entering checkout from pricing (e.g. demo, pro). */
export const CHECKOUT_PLAN_PARAM = 'plan';

/** Search param for billing interval when entering checkout from pricing (monthly, yearly). */
export const CHECKOUT_INTERVAL_PARAM = 'interval';
