fragment UserRow on UserObject {
  id
  createdAt
  disabledAt
  email
  githubUsername
  updatedAt
}

query getUsers {
  users {
    ...UserRow
  }
}

mutation createUser($input: CreateUserInput!) {
  createUser(input: $input) {
    ...UserRow
  }
}
