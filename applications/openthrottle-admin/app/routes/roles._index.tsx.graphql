fragment RoleRow on RoleObject {
  createdAt
  description
  id
  name
  permissions {
    id
    name
  }
  updatedAt
}

query getRoles {
  roles {
    ...RoleRow
  }
}

mutation createRole($input: CreateRoleInput!) {
  createRole(input: $input) {
    ...RoleRow
  }
}
