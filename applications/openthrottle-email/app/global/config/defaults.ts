/**
 * @description Global tunables for the mail app. Hoisted out of components per
 * component-primitive-shape R4 (configuration → config/).
 */

/** Debounce window (ms) for search-as-you-type navigation on the search page. Used by {@link useMailToolbar}. */
export const SEARCH_DEBOUNCE_MS = 300;
