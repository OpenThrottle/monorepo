/**
 * Cookie name the mail shell reads to restore the sidebar open/closed state
 * across navigations (written client-side by the shadcn SidebarProvider).
 */
export const SIDEBAR_COOKIE_NAME = 'sidebar_state';
