---
group: 01. Local Development
order: 2
title: What are the prerequisites?
---

Node.js **>= 22**, **pnpm** (this is a pnpm-only workspace — a `preinstall` guard blocks npm and yarn), and **Docker** for the local Postgres + Redis stack. Run `./scripts/setup.sh` from the workspace root for first-time setup.
