fragment ProjectCard on ProjectObject {
  createdAt
  description
  id
  name
  nxProjectName
  plans {
    title
  }
  tasks {
    title
  }
  updatedAt
}

query getProjects {
  projects {
    ...ProjectCard
  }
}
