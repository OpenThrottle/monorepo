import { describe, expect, test } from 'vitest';
// import { default as Route } from '../plans.$planId.tasks.edit.$taskId';

describe('routes/plans.$planId.tasks.edit.$taskId.tsx', () => {
  // let component: RenderResult;

  // beforeEach(() => {
  //   component = render(
  //     <RenderRouteWithOutletContext
  //       Route={Route}
  //       context={{}}
  //       initialEntries={[`/`]}
  //       path="/plans/:planId/tasks/:taskId/edit"
  //     />,
  //   );
  // });

  test('should render', async () => {
    expect(true).toBe(true);
  });
});
