import { describe, expect, test } from 'vitest';
// import { default as Route } from '../plans.tasks.$taskId';

describe('routes/plans.tasks.$taskId.tsx', () => {
  // let component: RenderResult;

  // beforeEach(() => {
  //   component = render(
  //     <RenderRouteWithOutletContext
  //       Route={Route}
  //       context={{}}
  //       initialEntries={[`/`]}
  //       path="/"
  //     />,
  //   );
  // });

  test('should render', async () => {
    expect(true).toBe(true);
  });
});
