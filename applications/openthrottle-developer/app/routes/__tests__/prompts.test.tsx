import { describe, test } from 'vitest';
// import Layout from '../prompts';
// import type { Route } from '@/app/routes/+types/prompts';

describe('routes/prompts.tsx', () => {
  // let component: RenderResult;

  // beforeEach(() => {
  //   component = render(
  //     <RenderRouteWithOutletContext
  //       Route={Route}
  //       context={{}}
  //       initialEntries={[`/auth`]}
  //       path="/auth"
  //     />,
  //   );
  // });

  test.skip('should render', () => {
    // Deferred: route integration render test (openthrottle-developer test hardening plan).
  });
});
