query getPullRequestDetail($input: GetPullInput!) {
  pull(input: $input) {
    author
    baseRef
    createdAt
    headRef
    htmlUrl
    mergedAt
    number
    state
    title
    updatedAt
  }
}
