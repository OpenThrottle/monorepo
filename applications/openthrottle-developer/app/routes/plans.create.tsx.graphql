# plans.$planId.create
mutation createPlan($input: CreatePlanInput!) {
  createPlan(input: $input) {
    assignee
    author
    category
    createdAt
    description
    id
    project
    projectId
    status
    summary
    title
    updatedAt
  }
}
