fragment PullRequestCard on PullListItemObject {
  author
  baseRef
  createdAt
  headRef
  headSha
  htmlUrl
  mergedAt
  number
  state
  title
  updatedAt
}

query getPullRequests($input: ListPullsInput!) {
  pulls(input: $input) {
    ...PullRequestCard
  }
}
