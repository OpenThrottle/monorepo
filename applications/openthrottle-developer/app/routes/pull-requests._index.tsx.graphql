fragment PullRequestCard on PullListItemObject {
  author
  createdAt
  htmlUrl
  mergedAt
  number
  state
  title
  updatedAt
}

query getPullRequests($input: ListPullsInput!) {
  pulls(input: $input) {
    ...PullRequestCard
  }
}
