query scheduleRepositoryOptions {
  workspaceLocalRepositories {
    displayName
    filesystemPath
    id
  }
}

mutation createScheduledAgentJob($input: CreateScheduledAgentJobInputType!) {
  createScheduledAgentJob(input: $input) {
    ...ScheduledJobCard
  }
}
