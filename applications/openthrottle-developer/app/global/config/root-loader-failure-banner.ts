/**
 * @description Tunables for the root-loader failure banner. Hoisted out of the
 * component per component-primitive-shape R4 (configuration → `config/`).
 */

/** Max characters of the failure message shown inline before collapsing behind a details toggle. */
export const ROOT_LOADER_FAILURE_SNIPPET_MAX = 360;
