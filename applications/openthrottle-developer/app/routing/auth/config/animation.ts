/** Resting value of the GradientMesh grain overlay before the exit tween. */
export const GRAIN_REST = 1.55;
/** How long the form fades out, in ms. Keep in sync with the `duration-700` class on the auth content wrapper. */
export const FORM_FADE_MS = 700;
/** Beat to hold on the bare gradient after the form fades, before navigating. */
export const HOLD_MS = 800;
