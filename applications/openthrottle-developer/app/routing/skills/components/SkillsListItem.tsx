import * as React from 'react';
import classnames from 'classnames';

export interface SkillsListItemProps {
  readonly className?: string;
}

export const SkillsListItem = (props: SkillsListItemProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="SkillsListItem">
      <h2>SkillsListItem</h2>
    </div>
  );
};
