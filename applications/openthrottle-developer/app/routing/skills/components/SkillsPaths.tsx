import * as React from 'react';
import classnames from 'classnames';

export interface SkillsPathsProps {
  readonly className?: string;
}

export const SkillsPaths = (props: SkillsPathsProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="SkillsPaths">
      <h2>SkillsPaths</h2>
    </div>
  );
};
