import * as React from 'react';
import classnames from 'classnames';

export interface SkillsOverviewProps {
  readonly className?: string;
}

export const SkillsOverview = (props: SkillsOverviewProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="SkillsOverview">
      <h2>SkillsOverview</h2>
    </div>
  );
};
