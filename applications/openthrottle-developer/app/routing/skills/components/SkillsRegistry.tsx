import * as React from 'react';
import classnames from 'classnames';

export interface SkillsRegistryProps {
  readonly className?: string;
}

export const SkillsRegistry = (props: SkillsRegistryProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="SkillsRegistry">
      <h2>SkillsRegistry</h2>
    </div>
  );
};
