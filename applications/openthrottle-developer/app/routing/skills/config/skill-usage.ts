/**
 * @description Config for the per-skill usage detail surface on /skills/$slug.
 * Hoisted out of the route module per the route primitive shape (R3).
 */

/** Fixed detail-route window, matching the /usage 30-day contract. */
export const SKILL_USAGE_RANGE_DAYS = 30;
