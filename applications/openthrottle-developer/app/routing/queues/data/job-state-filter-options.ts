/** Job states selectable from the detail toolbar and used as the loader default. */
export const QUEUE_JOB_STATE_FILTER_OPTIONS = [
  'waiting',
  'active',
  'completed',
  'failed',
  'delayed',
] as const;
