import * as React from 'react';
import classnames from 'classnames';

export interface QueueJobsTableProps {
  readonly className?: string;
}

export const QueueJobsTable = (props: QueueJobsTableProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="QueueJobsTable">
      <h2>QueueJobsTable</h2>
    </div>
  );
};
