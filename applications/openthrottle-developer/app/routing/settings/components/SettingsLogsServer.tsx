import * as React from 'react';
import classnames from 'classnames';

export interface SettingsLogsServerProps {
  readonly className?: string;
}

export const SettingsLogsServer = (props: SettingsLogsServerProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="SettingsLogsServer"
    >
      <h2>SettingsLogsServer</h2>
    </div>
  );
};
