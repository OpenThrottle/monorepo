import * as React from 'react';
import classnames from 'classnames';

export interface DashboardPlansByStatusProps {
  className?: string;
}

export const DashboardPlansByStatus = (props: DashboardPlansByStatusProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="DashboardPlansByStatus"
    >
      <h2>DashboardPlansByStatus</h2>
    </div>
  );
};
