/** Max size (bytes) accepted by the document-decompose upload (25 MiB). */
export const MAX_UPLOAD_BYTES = 25 * 1024 * 1024;

/** `accept` attribute for the document-decompose file input. */
export const ACCEPT_EXTENSIONS =
  '.csv,.htm,.html,.json,.md,.markdown,.xls,.xlsx,text/csv,text/html,text/markdown';
