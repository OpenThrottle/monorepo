import * as React from 'react';
import classnames from 'classnames';

export interface PlanToolbarSortByProps {
  readonly className?: string;
}

export const PlanToolbarSortBy = (props: PlanToolbarSortByProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="PlanToolbarSortBy"
    >
      <h2>PlanToolbarSortBy</h2>
    </div>
  );
};
