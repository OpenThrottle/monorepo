import * as React from 'react';
import classnames from 'classnames';

export interface PlanTasksProps {
  className?: string;
}

export const PlanTasks = (props: PlanTasksProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="PlanTasks">
      <h2>PlanTasks</h2>
    </div>
  );
};
