import * as React from 'react';
import classnames from 'classnames';

export interface PlanToolbarAssigneeProps {
  readonly className?: string;
}

export const PlanToolbarAssignee = (props: PlanToolbarAssigneeProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="PlanToolbarAssignee"
    >
      <h2>PlanToolbarAssignee</h2>
    </div>
  );
};
