import * as React from 'react';
import classnames from 'classnames';

export interface PlanStatsProps {
  readonly className?: string;
}

export const PlanStats = (props: PlanStatsProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="PlanStats">
      <h2>PlanStats</h2>
    </div>
  );
};
