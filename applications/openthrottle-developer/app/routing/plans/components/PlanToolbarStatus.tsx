import * as React from 'react';
import classnames from 'classnames';

export interface PlanToolbarStatusProps {
  readonly className?: string;
}

export const PlanToolbarStatus = (props: PlanToolbarStatusProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="PlanToolbarStatus"
    >
      <h2>PlanToolbarStatus</h2>
    </div>
  );
};
