import * as React from 'react';
import classnames from 'classnames';

export interface PlanFiltersProps {
  className?: string;
}

export const PlanFilters = (props: PlanFiltersProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="PlanFilters">
      <h2>PlanFilters</h2>
    </div>
  );
};
