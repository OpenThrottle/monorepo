import * as React from 'react';
import classnames from 'classnames';

export interface PlanTasksToolbarProps {
  readonly className?: string;
}

export const PlanTasksToolbar = (props: PlanTasksToolbarProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="PlanTasksToolbar"
    >
      <h2>PlanTasksToolbar</h2>
    </div>
  );
};
