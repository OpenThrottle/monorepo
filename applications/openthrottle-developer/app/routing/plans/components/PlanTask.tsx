import * as React from 'react';
import classnames from 'classnames';

export interface PlanTaskProps {
  className?: string;
}

export const PlanTask = (props: PlanTaskProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="PlanTask">
      <h2>PlanTask</h2>
    </div>
  );
};
