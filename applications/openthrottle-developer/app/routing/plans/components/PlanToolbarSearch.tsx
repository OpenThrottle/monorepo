import * as React from 'react';
import classnames from 'classnames';

export interface PlanToolbarSearchProps {
  readonly className?: string;
}

export const PlanToolbarSearch = (props: PlanToolbarSearchProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div
      className={classnames('p-4', className)}
      data-testid="PlanToolbarSearch"
    >
      <h2>PlanToolbarSearch</h2>
    </div>
  );
};
