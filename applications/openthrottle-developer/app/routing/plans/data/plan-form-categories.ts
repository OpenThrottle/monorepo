/**
 * @description Category options rendered by the {@link PlanForm} category
 * select. Hoisted from the component file per component-primitive-shape R4
 * (hardcoded data lives in the sibling data/ folder).
 */
export const PLAN_CATEGORIES = [
  'feature',
  'ideas',
  'infrastructure',
  'other',
  'product',
] as const;
