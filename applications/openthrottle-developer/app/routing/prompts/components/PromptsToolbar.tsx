import * as React from 'react';
import classnames from 'classnames';

export interface PromptsToolbarProps {
  className?: string;
}

export const PromptsToolbar = (props: PromptsToolbarProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="PromptsToolbar">
      <h2>PromptsToolbar</h2>
    </div>
  );
};
