import * as React from 'react';
import classnames from 'classnames';

export interface NotesTableProps {
  className?: string;
}

export const NotesTable = (props: NotesTableProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="NotesTable">
      <h2>NotesTable</h2>
    </div>
  );
};
