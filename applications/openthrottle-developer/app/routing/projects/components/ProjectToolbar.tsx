import * as React from 'react';
import classnames from 'classnames';

export interface ProjectToolbarProps {
  className?: string;
}

export const ProjectToolbar = (props: ProjectToolbarProps) => {
  const { className } = props;

  // Hooks

  // Setup

  // Handlers

  // Markup

  // Life Cycle

  // 🔌 Short Circuit

  return (
    <div className={classnames('p-4', className)} data-testid="ProjectToolbar">
      <h2>ProjectToolbar</h2>
    </div>
  );
};
