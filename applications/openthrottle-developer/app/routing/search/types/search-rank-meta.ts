/** Position context for “Why this result?” power-user labels. */
export interface SearchRankMeta {
  readonly indexOnPage: number;
  readonly page: number;
  readonly pageSize: number;
  readonly total: number;
}
