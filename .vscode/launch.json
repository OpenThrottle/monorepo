{
  "version": "0.2.0",
  "compounds": [
    {
      "name": "🚀 OpenThrottle.ai",
      "configurations": []
    }
  ],

  "configurations": [
    {
      "command": "npx ts-node-dev --transpile-only --respawn --inspect=4321 ${relativeFile}",
      "cwd": "${workspaceRoot}",
      "name": "File / TypeScript",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
